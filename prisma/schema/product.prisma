model Product {
  id         String   @id @default(uuid())
  name       String
  category   String
  price      Decimal 
  image      String
  featured   Boolean
  material   String
  dimensions String
  origin     String
  care       String
  createdAt  DateTime @default(now())
}
